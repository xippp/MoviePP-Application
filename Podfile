# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MoviePP-Application' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MoviePP-Application
	pod 'RxSwift'
	pod 'RxCocoa'
  pod 'Kingfisher'
  target 'MoviePP-ApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoviePP-ApplicationUITests' do
    # Pods for testing
  end

end
